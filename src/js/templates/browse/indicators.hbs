<!-- Indicators -->
{{#grouped_each 3 data.indicators}}
    <div class="row">
        {{#each this }}
            <div class="col-sm-4 adam-entry-point-content">
                <div class="row">
                    <div class="col-xs-7 indicator-name"><p>{{this.name}} ({{this.period}})<a href="#" name="{{this.footnote}}" class="anchor"><sup>{{this.footnote}}</sup></a></p></div>
                    <div class="col-xs-5 indicator-value"><p>{{commaSeparator this.value}}</p></div>
                </div>
            </div>
        {{/each}}
    </div>
{{/grouped_each}}

<div class="row">
    <hr/>
</div>

<div class="row">
    <div class="col-sm-11 current-sel-holder">
        <ul class="list-unstyled">
            <li ><b>{{data.source}}:</b></li>
            {{#each data.footnotes}}
            <li> <span id="{{this.footnote}}">{{this.footnote}}  {{this.source}} </span>  </li>
            {{/each}}

        </ul>
    </div>
</div>
